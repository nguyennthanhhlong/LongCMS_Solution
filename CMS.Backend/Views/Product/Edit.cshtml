@* * Sinh vien: Nguyen Thanh Long
 * Ma so: 2123110003
 * Ngay tao: 28-05-2026
 * Version: 1.0 
 *@

@model CMS.Data.Entities.Product
@{ Layout = "_LayoutAdmin"; ViewData["Title"] = "Cập nhật sản phẩm"; }

<div class="container mt-4">
    <div class="card shadow border-0">
        <div class="card-header bg-warning text-dark">
            <h5 class="mb-0"><i class="bi bi-pencil-square"></i> CẬP NHẬT THÔNG TIN SẢN PHẨM</h5>
        </div>
        <div class="card-body">
            <form asp-action="Edit" method="post" enctype="multipart/form-data">
                <input type="hidden" asp-for="Id" />
                <input type="hidden" asp-for="ImageUrl" />

                <div class="row">
                    <div class="col-md-8">
                        <div class="mb-3">
                            <label class="form-label fw-bold">Tên sản phẩm:</label>
                            <input asp-for="Name" class="form-control" required />
                        </div>
                        <div class="mb-3">
                            <label class="form-label fw-bold">Mô tả sản phẩm:</label>
                            <textarea asp-for="Description" class="form-control" rows="8"></textarea>
                        </div>
                    </div>
                    
                    <div class="col-md-4">
                        <div class="mb-3">
                            <label class="form-label fw-bold">Danh mục thuộc về:</label>
                            <select asp-for="CategoryProductId" class="form-select" asp-items="ViewBag.CategoryProductList" required></select>
                        </div>
                        <div class="mb-3">
                            <label class="form-label fw-bold">Giá bán (VNĐ):</label>
                            <input asp-for="Price" type="number" class="form-control" required />
                        </div>
                        <div class="mb-3">
                            <label class="form-label fw-bold">Số lượng tồn kho:</label>
                            <input asp-for="StockQuantity" type="number" class="form-control" required />
                        </div>
                        <div class="mb-3 text-center">
                            <label class="form-label fw-bold text-start w-100">Hình ảnh hiện tại:</label>
                            <img src="@Model.ImageUrl" style="width: 100%; max-height: 150px; object-fit: cover;" class="rounded border mb-2" alt="No image" />
                            <input type="file" name="uploadImage" class="form-control" accept="image/*" />
                            <small class="text-muted">Chỉ tải lên ảnh mới nếu muốn thay đổi.</small>
                        </div>
                        
                        <hr />
                        <button type="submit" class="btn btn-warning w-100 mb-2"><i class="bi bi-check2-circle"></i> Cập nhật thay đổi</button>
                        <a asp-action="Index" class="btn btn-secondary w-100">Quay lại</a>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>